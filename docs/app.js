const FALLBACK={incidents:[],updatedAt:null,source:'Cloudflare Radar'};
const state={...FALLBACK,filter:'all',map:null,layers:[],osintMap:null,osintMarker:null,userMarker:null};
const esc=s=>String(s??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[m]));

async function loadFeed(){
 try{const r=await fetch('data/incidents.json?ts='+Date.now(),{cache:'no-store'});if(!r.ok)throw 0;const data=await r.json();state.incidents=Array.isArray(data.incidents)?data.incidents:[];state.updatedAt=data.updatedAt||null;state.source=data.source||'Cloudflare Radar';document.querySelector('#feedStatus').textContent=state.incidents.length?'REAL TELEMETRY':'FEED WAITING';}
 catch(e){state.incidents=[];state.updatedAt=null;document.querySelector('#feedStatus').textContent='FEED OFFLINE';}
 renderAll();drawMap();
}
async function loadKEV(){const box=document.querySelector('#newsList');try{const r=await fetch('data/kev.json?ts='+Date.now(),{cache:'no-store'});if(!r.ok)throw 0;const data=await r.json();const rows=Array.isArray(data.vulnerabilities)?data.vulnerabilities:[];box.innerHTML=rows.slice(0,10).map(v=>`<div class="feed-row"><span><span class="feed-title">${esc(v.cveID||'CVE')}</span><span class="feed-meta">${esc(v.vulnerabilityName||'Known Exploited Vulnerability')}</span></span><span class="feed-meta">${esc(v.dateAdded||'')}</span></div>`).join('')||'<div class="incident empty">لا توجد سجلات KEV محمّلة.</div>';document.querySelector('#newsCount').textContent=rows.length;}catch(e){box.innerHTML='<div class="incident empty"><div class="incident-title">CISA KEV بانتظار المزامنة</div><div class="incident-meta"><span>REAL GOVERNMENT FEED</span><span>NO DEMO DATA</span></div></div>';document.querySelector('#newsCount').textContent='—';}}
function filteredIncidents(){if(state.filter==='all')return state.incidents;return state.incidents.filter(x=>{const t=String(x.type||'').toLowerCase(),tag=String(x.tag||'').toLowerCase();if(state.filter==='l7')return t==='l7'||tag.includes('l7');if(state.filter==='ddos')return t==='ddos'||tag.includes('ddos');if(state.filter==='network')return t==='network'||t==='l3'||tag.includes('network');return true;});}
function renderIncidents(){const rows=filteredIncidents(),box=document.querySelector('#incidents');if(!rows.length)box.innerHTML='<div class="incident empty"><div class="incident-title">لا توجد بيانات موثقة لهذا الفلتر</div><div class="incident-meta"><span>REAL DATA ONLY</span><span>—</span></div></div>';else box.innerHTML=rows.map(x=>`<div class="incident"><div class="incident-title">${esc(x.name||'Public threat telemetry')}</div><div class="incident-meta"><span>${esc(x.from)} → ${esc(x.to)}</span><span class="tag ${esc(x.type||'scan')}">${esc(x.tag||'PUBLIC')}</span></div><div class="incident-meta"><span>${esc(x.time||'آخر 24 ساعة')}</span><span>RADAR</span></div><div class="incident-meta"><span>${x.share!=null?esc(x.share)+'%':''}</span><span>MITIGATED SHARE</span></div></div>`).join('');document.querySelector('#incidentCount').textContent=state.incidents.length;document.querySelector('#countryCount').textContent=new Set(state.incidents.flatMap(x=>[x.fromCode,x.toCode].filter(Boolean))).size;}
function renderTicker(){document.querySelector('#tickerText').textContent=state.updatedAt?`آخر مزامنة: ${new Date(state.updatedAt).toLocaleString('ar')} · ${state.incidents.length} أزواج هجوم من ${state.source}`:'بانتظار مزامنة مصدر التهديد الحقيقي…';}
function renderAll(){renderIncidents();renderTicker();}

const CENTERS={US:[39.8,-98.6],CN:[35.9,104.2],NL:[52.1,5.3],DE:[51.2,10.4],BE:[50.8,4.5],FR:[46.2,2.2],GB:[55.4,-3.4],JP:[36.2,138.3],ID:[-2.2,117.4],IN:[22.8,79],SG:[1.35,103.8],AU:[-25.3,133.8],CA:[56.1,-106.3],BR:[-10.8,-51.9],RU:[61.5,105.3],PL:[52.1,19.1],IT:[42.8,12.8],ES:[40.3,-3.7],TR:[39,35.2],KR:[36.5,127.9],TW:[23.7,121],HK:[22.3,114.2],VN:[14.1,108.3],TH:[15.9,101],MY:[4.2,101.9],SE:[62,15],NO:[64.5,11],FI:[64,26],CH:[46.8,8.2],AT:[47.6,14.1],UA:[49,31.4],ZA:[-30.6,22.9],EG:[26.8,30.8],AE:[24.3,54.4],SA:[23.9,45.1],IL:[31,34.9],AR:[-38.4,-63.6],MX:[23.6,-102.6],LY:[26.3,17.2],TN:[33.9,9.5],DZ:[28,1.7],MA:[31.8,-7.1],NG:[9.1,8.7],KE:[0.2,37.9],PK:[30.4,69.3],BD:[23.7,90.4],IR:[32.4,53.7],IQ:[33.2,43.7],JO:[30.6,36.2],QA:[25.3,51.2],KW:[29.3,47.5],GR:[39.1,21.8],PT:[39.4,-8.2],RO:[45.9,25],CZ:[49.8,15.5],HU:[47.2,19.5],DK:[56,10],IE:[53.1,-8],NZ:[-40.9,174.9]};
function coords(x){if(Number.isFinite(+x.lat)&&Number.isFinite(+x.lon))return [+x.lat,+x.lon];if(Number.isFinite(+x.latitude)&&Number.isFinite(+x.longitude))return [+x.latitude,+x.longitude];return null;}
function popup(x,label){return `<b>${esc(x.name||'Threat telemetry')}</b><br>${esc(x.from||'')} → ${esc(x.to||'')}<br><small>${esc(x.time||'آخر 24 ساعة')} · ${esc(x.tag||'Public data')} · ${label}</small>`;}
function drawMap(){const el=document.querySelector('#worldMap');if(!el||!window.L)return;if(!state.map){state.map=L.map(el,{zoomControl:false,attributionControl:true,worldCopyJump:true}).setView([25,10],2);L.control.zoom({position:'bottomright'}).addTo(state.map);L.tileLayer('https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png',{maxZoom:18,attribution:'© OpenStreetMap © CARTO'}).addTo(state.map)}state.layers.forEach(l=>state.map.removeLayer(l));state.layers=[];filteredIncidents().forEach(x=>{const a=coords(x)||CENTERS[x.fromCode],b=coords(x)||CENTERS[x.toCode];if(!a||!b)return;const color=String(x.type).toLowerCase()==='l7'||String(x.tag).toLowerCase().includes('ddos')?'#ff5964':'#b28cff';const icon=L.divIcon({className:'',html:`<span class="pulse-dot" style="--pulse:${color};background:${color};box-shadow:0 0 14px ${color}"></span>`,iconSize:[12,12]});const from=L.marker(a,{icon}).addTo(state.map).bindPopup(popup(x,'SOURCE'));const to=L.marker(b,{icon}).addTo(state.map).bindPopup(popup(x,'TARGET'));const line=L.polyline([a,b],{color,weight:2,opacity:.85,dashArray:'8 10'}).addTo(state.map);state.layers.push(from,to,line);let offset=0;const timer=setInterval(()=>{if(!line._map){clearInterval(timer);return}offset=(offset+1)%18;line.setStyle({dashOffset:String(offset)});},80);});setTimeout(()=>state.map.invalidateSize(),100);}
function setupFilters(){document.querySelectorAll('.filter[data-filter]').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.filter[data-filter]').forEach(b=>b.classList.remove('active'));btn.classList.add('active');state.filter=btn.dataset.filter;renderIncidents();drawMap();}));document.querySelector('#refreshFeed')?.addEventListener('click',loadFeed);document.querySelector('#locateMe')?.addEventListener('click',locateUser);}

function readExif(data){const b=new Uint8Array(data);if(b[0]!==255||b[1]!==216)return null;let i=2;while(i+4<b.length){if(b[i]!==255){i++;continue}const marker=b[i+1];i+=2;if(marker===217||marker===218)break;const len=(b[i]<<8)|b[i+1];if(marker===225&&String.fromCharCode(...b.slice(i+2,i+8))==='Exif\0\0'){const start=i+8,view=new DataView(data),little=view.getUint16(start)===0x4949,u16=o=>view.getUint16(o,little),u32=o=>view.getUint32(o,little),ifd=start+u32(start+4),count=u16(ifd);let gpsPtr=0;for(let n=0;n<count;n++){const p=ifd+2+n*12;if(u16(p)===0x8825)gpsPtr=u32(p+8)}if(!gpsPtr)return {exif:true};const gps=start+gpsPtr,gcount=u16(gps),tags={};for(let n=0;n<gcount;n++){const p=gps+2+n*12;tags[u16(p)]={type:u16(p+2),count:u32(p+4),value:p+8}}const readRatios=t=>{const p=t.count<=2?t.value:start+u32(t.value);return Array.from({length:3},(_,k)=>{const q=p+k*8;return u32(q)/u32(q+4)})};const lat=tags[2],latRef=tags[1],lon=tags[4],lonRef=tags[3];if(lat&&lon){const la=readRatios(lat),lo=readRatios(lon);let latitude=la[0]+la[1]/60+la[2]/3600,longitude=lo[0]+lo[1]/60+lo[2]/3600;const ref=String.fromCharCode(view.getUint8(latRef.value)),ref2=String.fromCharCode(view.getUint8(lonRef.value));if(ref==='S')latitude=-latitude;if(ref2==='W')longitude=-longitude;return {exif:true,latitude,longitude,gps:true}}return {exif:true};}i+=len;}return null;}
async function reverseGeocode(lat,lon){try{const r=await fetch(`https://nominatim.openstreetmap.org/reverse?format=jsonv2&lat=${encodeURIComponent(lat)}&lon=${encodeURIComponent(lon)}&zoom=18&addressdetails=1`,{headers:{Accept:'application/json'}});if(!r.ok)throw 0;const d=await r.json();return d.display_name||null;}catch(e){return null;}}
function showOsintMap(lat,lon,label){const el=document.querySelector('#osintMap');if(!el||!window.L)return;if(!state.osintMap){state.osintMap=L.map(el,{zoomControl:true}).setView([lat,lon],15);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'© OpenStreetMap contributors'}).addTo(state.osintMap);}else state.osintMap.setView([lat,lon],15);if(state.osintMarker)state.osintMap.removeLayer(state.osintMarker);state.osintMarker=L.marker([lat,lon]).addTo(state.osintMap).bindPopup(`<b>${esc(label||'Detected location')}</b><br>${lat.toFixed(6)}, ${lon.toFixed(6)}`).openPopup();setTimeout(()=>state.osintMap.invalidateSize(),100);}
async function handleOsint(file){const input=document.querySelector('#imageInput'),result=document.querySelector('#osintResult'),preview=document.querySelector('#preview'),grid=document.querySelector('#exifGrid'),guess=document.querySelector('#locationGuess');if(!file||!file.type.startsWith('image/'))return;preview.src=URL.createObjectURL(file);result.classList.remove('hidden');const buf=await file.arrayBuffer(),exif=readExif(buf);let gps='غير موجود / غير مستخرج';let place='لم يتم تحديد مكان موثوق';if(exif?.gps){const lat=exif.latitude,lon=exif.longitude;gps=`${lat.toFixed(6)}, ${lon.toFixed(6)}`;guess.textContent='GPS موجود — جارٍ تحديد المكان';const name=await reverseGeocode(lat,lon);place=name||'تعذر تحويل الإحداثيات إلى اسم مكان';guess.textContent=name?`📍 ${name}`:'📍 إحداثيات GPS فقط';showOsintMap(lat,lon,name||'GPS');}else guess.textContent='لا يوجد GPS — لا نخمن موقعًا';grid.innerHTML=[['FILE',file.name],['TYPE',file.type],['SIZE',(file.size/1024/1024).toFixed(2)+' MB'],['GPS',gps],['PLACE',place],['ENGINE','EXIF + OpenStreetMap'],['MODE','LOCAL IMAGE / NO UPLOAD']].map(x=>`<div><small>${esc(x[0])}</small>${esc(x[1])}</div>`).join('');}
function setupOsint(){const input=document.querySelector('#imageInput'),zone=document.querySelector('#dropzone');input.addEventListener('change',e=>handleOsint(e.target.files[0]));['dragenter','dragover'].forEach(ev=>zone.addEventListener(ev,e=>{e.preventDefault();zone.classList.add('drag')}));['dragleave','drop'].forEach(ev=>zone.addEventListener(ev,e=>{e.preventDefault();zone.classList.remove('drag')}));zone.addEventListener('drop',e=>handleOsint(e.dataTransfer.files[0]));}
function locateUser(){if(!navigator.geolocation){alert('المتصفح لا يدعم تحديد الموقع.');return}navigator.geolocation.getCurrentPosition(p=>{const lat=p.coords.latitude,lon=p.coords.longitude;if(!state.map)return;if(state.userMarker)state.map.removeLayer(state.userMarker);state.userMarker=L.marker([lat,lon]).addTo(state.map).bindPopup(`<b>موقع جهازك</b><br>${lat.toFixed(5)}, ${lon.toFixed(5)}`).openPopup();state.map.setView([lat,lon],8);},()=>alert('لم يتم السماح بتحديد الموقع.'));}

document.querySelector('#loginBtn').addEventListener('click',()=>alert('تسجيل Google يحتاج مزود OAuth حقيقي. لا نخزن كلمات المرور داخل GitHub Pages.'));
setupFilters();setupOsint();loadFeed();loadKEV();